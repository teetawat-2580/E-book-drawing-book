<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Batch Edit & File Upload | คลังสมอง KLANGSAMONG</title>
    <!-- Google Fonts: Prompt & Kanit -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;500;600;700&family=Prompt:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --primary-navy: #1E3A8A;
            --sky-blue: #38BDF8;
            --fresh-green: #22C55E;
            --green-hover: #16a34a;
            --sun-yellow: #FACC15;
            --bg-light: #F3F4F6;
            --text-dark: #1E293B;
        }

        * { box-sizing: border-box; }
        body { font-family: 'Prompt', 'Kanit', sans-serif; background-color: var(--bg-light); margin: 0; padding: 25px 15px; color: var(--text-dark); }
        .batch-container { max-width: 1400px; background: white; padding: 30px; border-radius: 14px; box-shadow: 0 4px 20px rgba(0,0,0,0.06); margin: auto; border: 1px solid #e2e8f0; }
        h2 { color: var(--primary-navy); margin-top: 0; font-size: 21px; border-bottom: 2px solid #e2e8f0; padding-bottom: 14px; display: flex; justify-content: space-between; align-items: center; }
        .btn-nav { background: #f1f5f9; color: var(--primary-navy); padding: 7px 14px; border-radius: 20px; text-decoration: none; font-size: 13px; font-weight: 600; border: 1px solid #cbd5e1; }
        .btn-nav:hover { background: #e2e8f0; }
        
        .alert { padding: 12px 18px; border-radius: 8px; margin-bottom: 20px; font-size: 14px; font-weight: 600; }
        .alert-success { background: #dcfce7; border: 1px solid #86efac; color: #15803d; }
        .alert-error { background: #fef2f2; border: 1px solid #fca5a5; color: #ef4444; }

        /* Tabs */
        .tabs { display: flex; border-bottom: 2px solid #e2e8f0; margin-bottom: 20px; gap: 5px; }
        .tab-btn { padding: 12px 20px; border: none; background: none; font-size: 14.5px; font-weight: 700; color: #64748b; cursor: pointer; border-bottom: 3px solid transparent; transition: 0.2s; font-family: inherit; }
        .tab-btn.active { color: var(--primary-navy); border-bottom-color: var(--sky-blue); }

        .tab-content { display: none; }
        .tab-content.active { display: block; }

        textarea { width: 100%; height: 260px; font-family: monospace; font-size: 13.5px; padding: 12px; border: 1px solid #cbd5e1; border-radius: 8px; box-sizing: border-box; background: #f8fafc; }
        
        .btn-primary { background-color: var(--fresh-green); color: white; border: none; padding: 12px 25px; font-size: 15px; font-weight: 700; border-radius: 8px; cursor: pointer; margin-top: 15px; transition: background 0.2s; box-shadow: 0 4px 12px rgba(34, 197, 94, 0.3); font-family: inherit; }
        .btn-primary:hover { background-color: var(--green-hover); }

        .btn-secondary { background-color: #e0f2fe; color: #0369a1; border: 1px solid #bae6fd; padding: 8px 15px; font-size: 13px; font-weight: 700; border-radius: 6px; cursor: pointer; margin-bottom: 12px; font-family: inherit; }
        .btn-secondary:hover { background-color: #bae6fd; }

        /* Full Width Interactive Table */
        .table-responsive { overflow-x: auto; max-width: 100%; border: 1px solid #e2e8f0; border-radius: 8px; margin-top: 10px; max-height: 580px; overflow-y: auto; }
        .table-batch { width: 100%; border-collapse: collapse; min-width: 1650px; }
        .table-batch th { position: sticky; top: 0; background: var(--primary-navy); color: white; padding: 10px; font-size: 12.5px; font-weight: 700; border: 1px solid #1e293b; z-index: 10; text-align: left; }
        .table-batch td { border: 1px solid #e2e8f0; padding: 6px; font-size: 12px; background: white; vertical-align: top; }
        .table-batch tr:nth-child(even) td { background: #f8fafc; }
        .table-batch input, .table-batch select, .table-batch textarea { width: 100%; padding: 5px 7px; border: 1px solid #cbd5e1; border-radius: 4px; box-sizing: border-box; font-size: 12px; font-family: inherit; }
        .table-batch textarea { height: 35px; resize: vertical; }

        /* File Upload Picker Button inside cell */
        .btn-file-upload { display: inline-flex; align-items: center; justify-content: center; gap: 4px; background: #e0f2fe; color: #0369a1; border: 1px solid #7dd3fc; border-radius: 6px; padding: 4px 8px; font-size: 11.5px; font-weight: 700; cursor: pointer; text-decoration: none; width: 100%; transition: background 0.15s; }
        .btn-file-upload:hover { background: #bae6fd; }

        .hint-box { background: #f0f9ff; border-left: 4px solid var(--sky-blue); padding: 12px 15px; border-radius: 6px; margin-bottom: 15px; font-size: 13.5px; color: #0369a1; }
    </style>
</head>
<body>

    <div class="batch-container">
        <h2>
            <span>⚡ ระบบ Batch Upload & Batch Edit (คลังสมอง KLANGSAMONG)</span>
            <div>
                <a href="/admin/manage" class="btn-nav">← จัดการสื่อ</a>
                <a href="/admin/upload" class="btn-nav">➕ อัปโหลดรายเล่ม</a>
            </div>
        </h2>

        <% if (successMessage) { %>
            <div class="alert alert-success">✅ <%= successMessage %></div>
        <% } %>
        <% if (errorMessage) { %>
            <div class="alert alert-error">⚠️ <%= errorMessage %></div>
        <% } %>

        <div class="tabs">
            <button class="tab-btn active" onclick="switchTab('visualTab', this)">✏️ Interactive Table Input & Batch Edit Mode</button>
            <button class="tab-btn" onclick="switchTab('jsonTab', this)">📄 JSON Batch Import</button>
            <button class="tab-btn" onclick="switchTab('sqlTab', this)">⚡ SQL Code Batch Execution</button>
        </div>

        <!-- Tab 1: Interactive Table Input & Edit Mode -->
        <div id="visualTab" class="tab-content active">
            <div class="hint-box">
                🛠️ <strong>Batch Edit & Direct File Overwrite Mode (เฉพาะ Admin):</strong> ท่านสามารถกดปุ่ม <code>📁 เลือกไฟล์ใหม่ทับเดิม</code> ในช่อง รูปปก, ไฟล์ PDF ฉบับเต็ม หรือไฟล์ตัวอย่าง เพื่ออัปโหลดไฟล์ใหม่ขึ้นไปทับไฟล์เดิมได้ทันที โดยระบบจะอัปโหลดและอัปเดต URL อัตโนมัติเมื่อกด <strong>"บันทึก Batch Edit ทั้งหมด"</strong>
            </div>

            <form action="/admin/batch-update" method="POST" id="visualBatchForm">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <button type="button" class="btn-secondary" onclick="addTableRow()">➕ เพิ่มสื่อเล่มใหม่ (Add New Row)</button>
                    <span style="font-size:13px; color:#64748b;">มีทั้งหมด <strong><%= books ? books.length : 0 %></strong> รายการในตาราง</span>
                </div>

                <div class="table-responsive">
                    <table class="table-batch" id="batchTable">
                        <thead>
                            <tr>
                                <th style="width: 40px;">ID</th>
                                <th style="width: 140px;">ชื่อสื่อ (Title)</th>
                                <th style="width: 95px;">ผู้จัดทำ (Author)</th>
                                <th style="width: 105px;">สำนักพิมพ์</th>
                                <th style="width: 125px;">หมวดหมู่ (Category)</th>
                                <th style="width: 70px;">ราคาขาย</th>
                                <th style="width: 70px;">ราคาปกติ</th>
                                <th style="width: 90px;">ความยาว/หน้า</th>
                                <th style="width: 75px;">ประเภทไฟล์</th>
                                <th style="width: 80px;">ป้ายกำกับ</th>
                                <th style="width: 145px;">🖼️ รูปปก (Cover File)</th>
                                <th style="width: 145px;">📖 PDF ฉบับเต็ม (Book PDF)</th>
                                <th style="width: 145px;">📄 PDF ตัวอย่าง (Sample)</th>
                                <th style="width: 140px;">รายละเอียด (Description)</th>
                                <th style="width: 40px;">ลบ</th>
                            </tr>
                        </thead>
                        <tbody id="tableBody">
                            <% if (books && books.length > 0) { %>
                                <% books.forEach(book => { %>
                                    <tr>
                                        <td>
                                            <input type="text" class="v-id" value="<%= book.id %>" readonly style="background:#eee; text-align:center; font-weight:bold;">
                                        </td>
                                        <td><input type="text" class="v-title" value="<%= book.title %>" required></td>
                                        <td><input type="text" class="v-author" value="<%= book.author_name || 'คลังสมอง' %>"></td>
                                        <td><input type="text" class="v-publisher" value="<%= book.publisher || 'คลังสมอง' %>"></td>
                                        <td>
                                            <select class="v-category">
                                                <option value="สมุดระบายสีเด็ก" <%= book.category === 'สมุดระบายสีเด็ก' || book.category === 'สมุดระบายสี' ? 'selected' : '' %>>สมุดระบายสีเด็ก</option>
                                                <option value="คณิตศาสตร์" <%= book.category === 'คณิตศาสตร์' || book.category === 'ชีทคณิตอนุบาล ป.1' ? 'selected' : '' %>>คณิตศาสตร์</option>
                                                <option value="พื้นฐานการบวกเลข" <%= book.category === 'พื้นฐานการบวกเลข' ? 'selected' : '' %>>พื้นฐานการบวกเลข</option>
                                                <option value="ชีทฝึกเขียน" <%= book.category === 'ชีทฝึกเขียน' ? 'selected' : '' %>>ชีทฝึกเขียน</option>
                                                <option value="แฟลชการ์ด" <%= book.category === 'แฟลชการ์ด' || book.category === 'แฟลชการ์ด 2 ภาษา' ? 'selected' : '' %>>แฟลชการ์ด 2 ภาษา</option>
                                                <option value="เกมฝึกสมอง" <%= book.category === 'เกมฝึกสมอง' || book.category === 'เกมฝึกสมองเด็ก' ? 'selected' : '' %>>เกมฝึกสมองเด็ก</option>
                                                <option value="นิทานเด็ก AI" <%= book.category === 'นิทานเด็ก AI' ? 'selected' : '' %>>นิทานและแบบเรียน</option>
                                                <option value="สื่อครูตกแต่ง" <%= book.category === 'สื่อครูตกแต่ง' || book.category === 'สื่อครูตกแต่งห้องเรียน' ? 'selected' : '' %>>สื่อครูตกแต่งห้องเรียน</option>
                                            </select>
                                        </td>
                                        <td><input type="number" step="0.01" class="v-price" value="<%= book.price %>" required></td>
                                        <td><input type="number" step="0.01" class="v-original-price" value="<%= book.original_price || '' %>"></td>
                                        <td><input type="text" class="v-pages" value="<%= book.pages_count || '' %>"></td>
                                        <td><input type="text" class="v-file-type" value="<%= book.file_type || 'PDF' %>"></td>
                                        <td><input type="text" class="v-badge" value="<%= book.badge || '' %>"></td>
                                        
                                        <!-- Cover File Picker Cell -->
                                        <td>
                                            <input type="hidden" class="v-cover" value="<%= book.cover_image_url || '' %>">
                                            <label class="btn-file-upload">
                                                📁 อัปโหลดรูปทับเดิม
                                                <input type="file" accept="image/*" onchange="uploadRowFile(this, 'covers', 'v-cover')" style="display:none;">
                                            </label>
                                            <div class="status-indicator" style="margin-top:3px;">
                                                <% if (book.cover_image_url) { %>
                                                    <a href="<%= book.cover_image_url %>" target="_blank" style="color:#0284c7; font-size:11px; text-decoration:none;">👁️ ดูภาพเดิม</a>
                                                <% } else { %>
                                                    <small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small>
                                                <% } %>
                                            </div>
                                        </td>

                                        <!-- Full PDF File Picker Cell -->
                                        <td>
                                            <input type="hidden" class="v-book-file" value="<%= book.file_path || '' %>">
                                            <label class="btn-file-upload">
                                                📁 อัปโหลด PDF ทับเดิม
                                                <input type="file" accept=".pdf" onchange="uploadRowFile(this, 'books', 'v-book-file')" style="display:none;">
                                            </label>
                                            <div class="status-indicator" style="margin-top:3px;">
                                                <% if (book.file_path) { %>
                                                    <a href="<%= book.file_path %>" target="_blank" style="color:#0284c7; font-size:11px; text-decoration:none;">📖 เปิด PDF เดิม</a>
                                                <% } else { %>
                                                    <small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small>
                                                <% } %>
                                            </div>
                                        </td>

                                        <!-- Sample PDF File Picker Cell -->
                                        <td>
                                            <input type="hidden" class="v-sample-file" value="<%= book.sample_file_path || '' %>">
                                            <label class="btn-file-upload">
                                                📁 อัปโหลดตัวอย่างทับเดิม
                                                <input type="file" accept=".pdf" onchange="uploadRowFile(this, 'samples', 'v-sample-file')" style="display:none;">
                                            </label>
                                            <div class="status-indicator" style="margin-top:3px;">
                                                <% if (book.sample_file_path) { %>
                                                    <a href="<%= book.sample_file_path %>" target="_blank" style="color:#0284c7; font-size:11px; text-decoration:none;">📄 เปิดตัวอย่างเดิม</a>
                                                <% } else { %>
                                                    <small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small>
                                                <% } %>
                                            </div>
                                        </td>

                                        <td><textarea class="v-description"><%= book.description || '' %></textarea></td>
                                        <td style="text-align:center;"><button type="button" style="color:red; cursor:pointer; border:none; background:none;" onclick="removeRow(this)">❌</button></td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td><input type="text" class="v-id" value="" placeholder="NEW" readonly style="background:#eee; text-align:center;"></td>
                                    <td><input type="text" class="v-title" placeholder="เล่ม 1 : สัตว์แสนรัก" required></td>
                                    <td><input type="text" class="v-author" placeholder="คลังสมอง"></td>
                                    <td><input type="text" class="v-publisher" placeholder="คลังสมอง"></td>
                                    <td>
                                        <select class="v-category">
                                            <option value="สมุดระบายสีเด็ก">สมุดระบายสีเด็ก</option>
                                            <option value="คณิตศาสตร์">คณิตศาสตร์</option>
                                            <option value="ชีทฝึกเขียน">ชีทฝึกเขียน</option>
                                            <option value="แฟลชการ์ด">แฟลชการ์ด 2 ภาษา</option>
                                            <option value="เกมฝึกสมอง">เกมฝึกสมองเด็ก</option>
                                            <option value="นิทานเด็ก AI">นิทานและแบบเรียน</option>
                                            <option value="สื่อครูตกแต่ง">สื่อครูตกแต่งห้องเรียน</option>
                                        </select>
                                    </td>
                                    <td><input type="number" step="0.01" class="v-price" placeholder="159" required></td>
                                    <td><input type="number" step="0.01" class="v-original-price" placeholder="199"></td>
                                    <td><input type="text" class="v-pages" placeholder="20 หน้า"></td>
                                    <td><input type="text" class="v-file-type" value="PDF"></td>
                                    <td><input type="text" class="v-badge" placeholder="แนะนำ"></td>
                                    
                                    <td>
                                        <input type="hidden" class="v-cover" value="">
                                        <label class="btn-file-upload">
                                            📁 อัปโหลดรูปใหม่
                                            <input type="file" accept="image/*" onchange="uploadRowFile(this, 'covers', 'v-cover')" style="display:none;">
                                        </label>
                                        <div class="status-indicator" style="margin-top:3px;"><small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small></div>
                                    </td>

                                    <td>
                                        <input type="hidden" class="v-book-file" value="">
                                        <label class="btn-file-upload">
                                            📁 อัปโหลด PDF ใหม่
                                            <input type="file" accept=".pdf" onchange="uploadRowFile(this, 'books', 'v-book-file')" style="display:none;">
                                        </label>
                                        <div class="status-indicator" style="margin-top:3px;"><small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small></div>
                                    </td>

                                    <td>
                                        <input type="hidden" class="v-sample-file" value="">
                                        <label class="btn-file-upload">
                                            📁 อัปโหลดตัวอย่างใหม่
                                            <input type="file" accept=".pdf" onchange="uploadRowFile(this, 'samples', 'v-sample-file')" style="display:none;">
                                        </label>
                                        <div class="status-indicator" style="margin-top:3px;"><small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small></div>
                                    </td>

                                    <td><textarea class="v-description" placeholder="รายละเอียด..."></textarea></td>
                                    <td style="text-align:center;"><button type="button" style="color:red; cursor:pointer; border:none; background:none;" onclick="removeRow(this)">❌</button></td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <input type="hidden" name="json_data" id="visualJsonOutput">
                <button type="submit" class="btn-primary" onclick="prepareVisualSubmit()">💾 บันทึก Batch Edit ทั้งหมด (Save All Batch Edits)</button>
            </form>
        </div>

        <!-- Tab 2: JSON Batch Input -->
        <div id="jsonTab" class="tab-content">
            <div class="hint-box">
                💡 <strong>นำเข้า JSON Batch:</strong> วาง JSON Array เพื่อเพิ่มสื่อหลายเล่มพร้อมกัน
            </div>
            <button type="button" class="btn-secondary" onclick="fillJsonDemo()">✨ เติมข้อมูลตัวอย่าง (Load Demo JSON)</button>
            <form action="/admin/batch-json" method="POST">
                <textarea name="json_data" id="jsonDataInput" placeholder='[&#10;  {&#10;    "title": "เล่ม 1 : สัตว์แสนรัก (สมุดระบายสี)",&#10;    "author_name": "คลังสมอง",&#10;    "publisher": "คลังสมอง KLANGSOMONG",&#10;    "category": "สมุดระบายสีเด็ก",&#10;    "price": 159,&#10;    "original_price": 199,&#10;    "pages_count": "20 หน้า",&#10;    "file_type": "PDF",&#10;    "cover_image_url": "/uploads/covers/cover.jpg",&#10;    "file_path": "/uploads/books/full.pdf",&#10;    "sample_file_path": "/uploads/samples/preview.pdf"&#10;  }&#10;]'></textarea>
                <button type="submit" class="btn-primary">🚀 นำเข้าข้อมูล Batch (Batch Insert)</button>
            </form>
        </div>

        <!-- Tab 3: SQL Batch Execution -->
        <div id="sqlTab" class="tab-content">
            <div class="hint-box">
                💡 <strong>รัน SQL Code โดยตรง:</strong> วางคำสั่ง <code>INSERT INTO books ...</code> หรือ <code>UPDATE books ...</code>
            </div>
            <button type="button" class="btn-secondary" onclick="fillSqlDemo()">✨ เติมตัวอย่างคำสั่ง SQL (Load Demo SQL)</button>
            <form action="/admin/batch-sql" method="POST">
                <textarea name="sql_query" id="sqlQueryInput" placeholder="INSERT INTO books (title, author_name, publisher, category, price, original_price, pages_count, description) VALUES ..."></textarea>
                <button type="submit" class="btn-primary" onclick="return confirm('ยืนยันการรันคำสั่ง SQL เข้าสู่ฐานข้อมูล?');">⚡ ประมวลผลคำสั่ง SQL (Execute SQL Batch)</button>
            </form>
        </div>

    </div>

    <script>
        function switchTab(tabId, btn) {
            document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
            document.querySelectorAll('.tab-btn').forEach(el => el.classList.remove('active'));
            document.getElementById(tabId).classList.add('active');
            btn.classList.add('active');
        }

        // AJAX File Upload function for direct file inputs in table cells
        async function uploadRowFile(fileInput, subFolder, hiddenInputClass) {
            if (!fileInput.files || fileInput.files.length === 0) return;

            const file = fileInput.files[0];
            const td = fileInput.closest('td');
            const hiddenInput = td.querySelector('.' + hiddenInputClass);
            const statusEl = td.querySelector('.status-indicator');

            statusEl.innerHTML = '<span style="color:#d97706; font-size:11px; font-weight:bold;">⏳ กำลังอัปโหลด...</span>';

            const formData = new FormData();
            formData.append('file', file);
            formData.append('subFolder', subFolder);

            try {
                const response = await fetch('/api/upload-file', {
                    method: 'POST',
                    body: formData
                });
                const result = await response.json();

                if (result.success && result.file_url) {
                    hiddenInput.value = result.file_url;
                    statusEl.innerHTML = `<span style="color:#16a34a; font-size:11px; font-weight:bold;">✅ อัปโหลดทับสำเร็จ!</span> <a href="${result.file_url}" target="_blank" style="color:#0284c7; font-size:11px; text-decoration:none;">👁️ เปิดดู</a>`;
                } else {
                    statusEl.innerHTML = `<span style="color:#dc2626; font-size:11px; font-weight:bold;">❌ ${result.message || 'ล้มเหลว'}</span>`;
                }
            } catch (err) {
                statusEl.innerHTML = `<span style="color:#dc2626; font-size:11px; font-weight:bold;">❌ อัปโหลดผิดพลาด</span>`;
                console.error(err);
            }
        }

        function fillJsonDemo() {
            const demo = [
                {
                    "title": "เล่ม 1 : สัตว์แสนรัก (สมุดระบายสี)",
                    "author_name": "คลังสมอง",
                    "publisher": "คลังสมอง KLANGSOMONG",
                    "category": "สมุดระบายสีเด็ก",
                    "price": 159,
                    "original_price": 199,
                    "pages_count": "20 หน้า",
                    "file_type": "PDF",
                    "badge": "ยอดนิยม",
                    "cover_image_url": "/uploads/covers/animal.jpg",
                    "file_path": "/uploads/books/animal-full.pdf",
                    "sample_file_path": "/uploads/samples/animal-preview.pdf",
                    "description": "สมุดระบายสีสำหรับเด็กเสริมสร้างจินตนาการ..."
                }
            ];
            document.getElementById('jsonDataInput').value = JSON.stringify(demo, null, 2);
        }

        function fillSqlDemo() {
            const sql = `INSERT INTO books (title, author_name, publisher, category, price, original_price, pages_count, file_type, badge, cover_image_url, file_path, sample_file_path) 
VALUES 
('เล่ม 1 : สัตว์แสนรัก', 'คลังสมอง', 'คลังสมอง KLANGSOMONG', 'สมุดระบายสีเด็ก', 159.00, 199.00, '20 หน้า', 'PDF', 'ยอดนิยม', '/uploads/covers/cover1.jpg', '/uploads/books/book1.pdf', '/uploads/samples/sample1.pdf');`;
            document.getElementById('sqlQueryInput').value = sql;
        }

        function addTableRow() {
            const tbody = document.getElementById('tableBody');
            const tr = document.createElement('tr');
            tr.innerHTML = `
                <td><input type="text" class="v-id" value="" placeholder="NEW" readonly style="background:#eee; text-align:center;"></td>
                <td><input type="text" class="v-title" placeholder="ชื่อสื่อ" required></td>
                <td><input type="text" class="v-author" placeholder="คลังสมอง"></td>
                <td><input type="text" class="v-publisher" placeholder="คลังสมอง"></td>
                <td>
                    <select class="v-category">
                        <option value="สมุดระบายสีเด็ก">สมุดระบายสีเด็ก</option>
                        <option value="คณิตศาสตร์">คณิตศาสตร์</option>
                        <option value="พื้นฐานการบวกเลข">พื้นฐานการบวกเลข</option>
                        <option value="ชีทฝึกเขียน">ชีทฝึกเขียน</option>
                        <option value="แฟลชการ์ด">แฟลชการ์ด 2 ภาษา</option>
                        <option value="เกมฝึกสมอง">เกมฝึกสมองเด็ก</option>
                        <option value="นิทานเด็ก AI">นิทานและแบบเรียน</option>
                        <option value="สื่อครูตกแต่ง">สื่อครูตกแต่งห้องเรียน</option>
                    </select>
                </td>
                <td><input type="number" step="0.01" class="v-price" placeholder="159" required></td>
                <td><input type="number" step="0.01" class="v-original-price" placeholder="199"></td>
                <td><input type="text" class="v-pages" placeholder="20 หน้า"></td>
                <td><input type="text" class="v-file-type" value="PDF"></td>
                <td><input type="text" class="v-badge" placeholder="แนะนำ"></td>
                
                <td>
                    <input type="hidden" class="v-cover" value="">
                    <label class="btn-file-upload">
                        📁 อัปโหลดรูปใหม่
                        <input type="file" accept="image/*" onchange="uploadRowFile(this, 'covers', 'v-cover')" style="display:none;">
                    </label>
                    <div class="status-indicator" style="margin-top:3px;"><small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small></div>
                </td>

                <td>
                    <input type="hidden" class="v-book-file" value="">
                    <label class="btn-file-upload">
                        📁 อัปโหลด PDF ใหม่
                        <input type="file" accept=".pdf" onchange="uploadRowFile(this, 'books', 'v-book-file')" style="display:none;">
                    </label>
                    <div class="status-indicator" style="margin-top:3px;"><small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small></div>
                </td>

                <td>
                    <input type="hidden" class="v-sample-file" value="">
                    <label class="btn-file-upload">
                        📁 อัปโหลดตัวอย่างใหม่
                        <input type="file" accept=".pdf" onchange="uploadRowFile(this, 'samples', 'v-sample-file')" style="display:none;">
                    </label>
                    <div class="status-indicator" style="margin-top:3px;"><small style="color:#94a3b8; font-size:11px;">ไม่มีไฟล์</small></div>
                </td>

                <td><textarea class="v-description" placeholder="รายละเอียด..."></textarea></td>
                <td style="text-align:center;"><button type="button" style="color:red; cursor:pointer; border:none; background:none;" onclick="removeRow(this)">❌</button></td>
            `;
            tbody.insertBefore(tr, tbody.firstChild);
        }

        function removeRow(btn) {
            const tr = btn.closest('tr');
            if (confirm('ต้องการลบแถวนี้ออกจากตารางแก้ไขหรือไม่?')) {
                tr.remove();
            }
        }

        function prepareVisualSubmit() {
            const rows = document.querySelectorAll('#tableBody tr');
            const data = [];
            rows.forEach(tr => {
                const id = tr.querySelector('.v-id') ? tr.querySelector('.v-id').value : '';
                const title = tr.querySelector('.v-title').value;
                const author = tr.querySelector('.v-author').value;
                const publisher = tr.querySelector('.v-publisher').value;
                const category = tr.querySelector('.v-category').value;
                const price = tr.querySelector('.v-price').value;
                const originalPrice = tr.querySelector('.v-original-price').value;
                const pages = tr.querySelector('.v-pages').value;
                const fileType = tr.querySelector('.v-file-type').value;
                const badge = tr.querySelector('.v-badge').value;
                const cover = tr.querySelector('.v-cover').value;
                const bookFile = tr.querySelector('.v-book-file').value;
                const sampleFile = tr.querySelector('.v-sample-file').value;
                const description = tr.querySelector('.v-description').value;

                if (title && title.trim()) {
                    data.push({
                        id: id ? parseInt(id) : null,
                        title: title,
                        author_name: author,
                        publisher: publisher,
                        category: category,
                        price: parseFloat(price) || 0,
                        original_price: originalPrice ? parseFloat(originalPrice) : null,
                        pages_count: pages,
                        file_type: fileType,
                        badge: badge,
                        cover_image_url: cover,
                        file_path: bookFile,
                        sample_file_path: sampleFile,
                        description: description
                    });
                }
            });
            document.getElementById('visualJsonOutput').value = JSON.stringify(data);
        }
    </script>
</body>
</html>
